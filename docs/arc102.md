# ARC102 - Store, Process, and Manage Data on Google Cloud - Command Line: Challenge Lab

Runbook untuk [`arc102.sh`](../arc102.sh). Status: terverifikasi 100/100 (2026-08-22, us-east1).

Kembaran [ARC100](arc100.md) — sama-sama bucket + topic + function pembuat
thumbnail — tapi **kodenya beda generasi**, dan itu menentukan cara deploy-nya.

## Sebelum jalan

1. Start lab, login student, buka Cloud Shell.
2. Baca **Task 2**: nama topic berakhiran angka acak per instance
   (`wild-topic-618`, ...). Wajib diisi.
3. Nama bucket mengikuti pola `wild-bucket-<PROJECT_ID>`, jadi default script
   sudah benar.

## Jalankan

```bash
curl -sLO https://raw.githubusercontent.com/ravi-arnan/gsp_lab_solutions/main/arc102.sh
TOPIC=wild-topic-<NNN> bash arc102.sh
```

4-6 menit, hampir semuanya di deploy.

## Yang dikerjakan script

| Task | Aksi | Checkpoint |
|------|------|------------|
| 1 | `buckets create gs://wild-bucket-<PROJECT_ID>` | ✅ Create a bucket |
| 2 | `pubsub topics create <TOPIC>` | ✅ Create a Pub/Sub topic |
| 3 | tulis `index.js` + `package.json`, deploy gen1, upload `wildlife.jpg`, tunggu thumbnail | ✅ Verify the Cloud Function worked |

Nama topic menggantikan `REPLACE_WITH_YOUR_TOPIC ID` di baris 15 `index.js`,
disulam lewat heredoc yang menginterpolasi `$TOPIC`.

## Kenapa gen1, padahal ARC100 gen2

`--no-gen2` di sini bukan gaya lama yang tertinggal, melainkan syarat:

1. **Tanda tangan fungsinya gaya lama.** `exports.thumbnail = (event, context)`
   adalah background function gen1. Gen2 memakai CloudEvent
   (`functions.cloudEvent('nama', cloudEvent => ...)` seperti di ARC100), jadi
   kode lab apa adanya tidak akan jalan kalau dipaksa gen2.
2. **`imagemagick-stream` memanggil binary `convert`.** Runtime gen1 menyediakan
   ImageMagick; buildpack gen2 tidak. Akibatnya fungsi tetap ter-deploy dan
   statusnya sehat, tapi thumbnail tidak pernah terbentuk dan checkpoint 3 tidak
   pernah hijau — kegagalan diam-diam yang mahal waktunya kalau tidak diketahui
   dari awal.

Perhatikan juga gen1 tidak butuh kerumitan IAM Eventarc seperti ARC100: trigger
`google.storage.object.finalize` memakai notifikasi GCS internal, bukan Eventarc,
jadi keempat binding di runbook ARC100 tidak diperlukan di sini.

## Klik manual

Tiga checkpoint. Script sudah mengunggah `wildlife.jpg` dan menunggu file
`*64x64_thumbnail.jpg` muncul, jadi kegagalan trigger ketahuan sebelum mengklik.

## Troubleshooting

| Gejala | Sebab dan solusi |
|--------|------------------|
| Log berisi `convert: not found` | Fungsi ter-deploy sebagai gen2. `gcloud functions delete <nama> --region=<region> --quiet` lalu jalankan ulang script. |
| Deploy gagal di percobaan pertama | Cloud Build atau Artifact Registry belum siap. Script mengulang sekali setelah 60 detik. |
| Thumbnail tidak muncul | `gcloud functions logs read wild-thumbnail-maker --region=<region> --limit=20`. |
| Publish ke Pub/Sub gagal | Nama topic di `index.js` tidak cocok. Pastikan `TOPIC` persis seperti teks Task 2. |
